

" Makes it so weird things don't happen...?
"set nocompatible

" Syntax highlighting
syntax on
		
colo industry

" Makes backspace work again since it broke for some reason
set backspace=2

set tabstop=4

set number

set ruler

set colorcolumn=80
